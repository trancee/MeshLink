# Git Branching Workflow Extension Configuration
# Copied to .specify/extensions/git/git-config.yml on install

# Branch numbering strategy: "sequential" (001, 002, ...) or "timestamp" (YYYYMMDD-HHMMSS)
branch_numbering: sequential

# Commit message used by `git commit` during repository initialization
init_commit_message: "chore: initial commit from Specify template"

# Auto-commit before/after core commands.
# Set "default" to enable for all commands, then override per-command.
# Each key can be true/false. Message is customizable per-command.
auto_commit:
  default: false
  before_clarify:
    enabled: false
    message: "chore: save progress before clarify"
  before_plan:
    enabled: false
    message: "chore: save progress before plan"
  before_tasks:
    enabled: false
    message: "chore: save progress before tasks"
  before_implement:
    enabled: false
    message: "chore: save progress before implement"
  before_checklist:
    enabled: false
    message: "chore: save progress before checklist"
  before_analyze:
    enabled: false
    message: "chore: save progress before analyze"
  before_taskstoissues:
    enabled: false
    message: "chore: save progress before issue sync"
  after_constitution:
    enabled: false
    message: "docs: add project constitution"
  after_specify:
    enabled: false
    message: "docs: add specification"
  after_clarify:
    enabled: false
    message: "docs: clarify specification"
  after_plan:
    enabled: false
    message: "docs: add implementation plan"
  after_tasks:
    enabled: false
    message: "docs: add tasks"
  after_implement:
    enabled: false
    message: "chore: implementation progress"
  after_checklist:
    enabled: false
    message: "docs: add checklist"
  after_analyze:
    enabled: false
    message: "docs: add analysis report"
  after_taskstoissues:
    enabled: false
    message: "chore: sync tasks to issues"

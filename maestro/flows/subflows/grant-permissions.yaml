appId: io.meshlink.sample
name: Grant Permissions
tags:
  - subflow
---
# ── Android 12+ BLE permission dialogs ──
# Use optional taps instead of conditional runFlow — avoids 7s timeout per check.
- runFlow:
    when:
      platform: Android
    commands:
      - tapOn:
          text: "Allow"
          optional: true
          label: "Grant Bluetooth scan permission"
      - tapOn:
          text: "Allow"
          optional: true
          label: "Grant Bluetooth connect permission"
      - tapOn:
          text: "Allow"
          optional: true
          label: "Grant Bluetooth advertise permission"
      - tapOn:
          text: "While using the app"
          optional: true
          label: "Grant location permission"
      - tapOn:
          text: "Precise"
          optional: true
          label: "Select precise location"

# ── iOS Bluetooth permission dialog ──
- runFlow:
    when:
      platform: iOS
    commands:
      - tapOn:
          text: "OK"
          optional: true
          label: "Grant Bluetooth permission"

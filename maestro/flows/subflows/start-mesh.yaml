appId: io.meshlink.sample
name: Start Mesh
tags:
  - subflow
---
- tapOn: "Start Mesh"
# Permissions are already granted (app is pre-installed by maestro-runner).
# On first launch with clearState, call grant-permissions.yaml manually.
- assertVisible: "Stop Mesh"
